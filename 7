minimized = false;

toggleMinimize() {
  this.minimized = !this.minimized;

  const container = document.querySelector('.mat-mdc-dialog-container') as HTMLElement;
  if (container) {
    container.style.display = this.minimized ? 'none' : 'block';
  }
}
