valueTooltips?: Record<string, string>;
field.valueTooltips = {}; // init

field.values.forEach(v => {
  const basketFilter = filters['basket_name'];
  if (basketFilter) {
    const baskets = basketFilter.split(',');
    field.valueTooltips![v] = `Available for: ${baskets.join(', ')}`;
  } else {
    field.valueTooltips![v] = 'Available';
  }
});

<mat-option *ngFor="let val of field.values"
            [value]="val"
            [matTooltip]="field.valueTooltips?.[val]">
  {{ val }}
</mat-option>
