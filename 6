filterOptions(field: any): string[] {
  const search = (field.searchText || '').toLowerCase();

  return (field.values || [])
    .filter(opt => !field.value.includes(opt)) // remove already selected
    .filter(opt => opt.toLowerCase().includes(search)); // apply search filter
}
