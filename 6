     field['valueTooltips'] = {};
      field.values.forEach(val => {
        field['valueTooltips'][val] = Object.keys(filters)
          .filter(fKey => filters[fKey].split(',').includes(val))
          .join(', ') || 'Available'; // fallback text if no parent matches
      });
